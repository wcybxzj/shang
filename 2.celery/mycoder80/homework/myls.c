#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <string.h>

int main(int argc, const char *argv[])
{
	while (1) {
		ch = getopt(argc, argv, "-lian");
		if (ch < 0) {
			break;
		}

		switch(ch){
			case 'l':

				break;
			case 'l':
				break;
			case 'l':
				break;
			case 'l':
				break;
			default:
				break;
		}


	}


	return 0;
}
