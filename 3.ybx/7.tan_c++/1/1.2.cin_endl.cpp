#include <iostream>

using namespace  std;
int main(int argc, const char *argv[])
{
	int a, b, sum;
	cin >> a >> b;
	sum = a+b;
	//cout << "a+b=" << sum << "\n";
	cout << "a+b=" << sum << endl;//endl 相当于换行
	sleep(3);
	return 0;
}
